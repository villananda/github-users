<resources>
    <!-- Base application theme. -->
    <style name="Base.Theme.GithubUsers" parent="Theme.Material3.Dark.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="colorPrimary">@color/gray_darker</item>
        <item name="android:textColorPrimary">@android:color/white</item>
    </style>

    <style name="Theme.GithubUsers.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.GithubUsers.PopupOverlay" parent="ThemeOverlay.AppCompat.Dark" />

    <style name="Theme.GithubUsers.CustomThemeAlertDialog" parent="Theme.MaterialComponents.DayNight.Dialog.Alert">
        <!--Background color-->
        <item name="android:background">#0000ff</item>

        <!--tittle color-->
        <item name="android:textColor">#000000</item>

        <!--text color of message-->
        <item name="android:textColorPrimary">#ff0000</item>

        <!--text color of buttons-->
        <item name="colorPrimary">#FFFF00</item>
    </style>
</resources>